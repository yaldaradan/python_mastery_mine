import 'package:flutter/material.dart';

class PatientRegistrationScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Patient Registration"),
      ),
      body: Center(
        child: Text("Patient Registration Page"),
      ),
    );
  }
}