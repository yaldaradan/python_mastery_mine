import 'package:flutter/material.dart';

class PharmacyRegistrationScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Pharmacy Registration"),
      ),
      body: Center(
        child: Text("Pharmacy Registration Page"),
      ),
    );
  }
}