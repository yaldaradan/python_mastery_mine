import 'package:flutter/material.dart';

class LabRegistrationScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Lab Registration"),
      ),
      body: Center(
        child: Text("Lab Registration Page"),
      ),
    );
  }
}